name: Worker uplift task
description: Track repo-local worker-uplift implementation work.
title: "[Task] "
labels:
  - worker-uplift
body:
  - type: markdown
    attributes:
      value: |
        Repository boundary: Consume persistence jobs and idempotently upsert translated article, approval, translation, and processing metadata into shadow-safe storage by default.
  - type: textarea
    id: goal
    attributes:
      label: Goal
      description: What should change?
    validations:
      required: true
  - type: textarea
    id: acceptance
    attributes:
      label: Acceptance criteria
      description: How will this be verified?
    validations:
      required: true
  - type: checkboxes
    id: guardrails
    attributes:
      label: Guardrails
      options:
        - label: No production secret is added to this repository.
          required: true
        - label: The legacy nutsnews-worker ingestion path remains untouched.
          required: true
